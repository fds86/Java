// Package declaration: organizza le classi in un namespace.
package Progetto_5_1;

/**
 * Classe principale dell'applicazione di esempio.
 * Contiene il metodo `main` che è il punto di ingresso quando si esegue
 * l'applicazione dalla riga di comando o da un JAR eseguibile.
 */
public class App {

    /**
     * Metodo principale (entry point).
     *
     * @param args argomenti passati da linea di comando
     * @throws Exception per semplicità qui dichiariamo una throwable generica;
     *                   in programmi reali è preferibile gestire le eccezioni
     *                   in modo più specifico o rimuovere il throws se non
     *                   necessario.
     */
    public static void main(String[] args) throws Exception {
        // Stampa un messaggio di esempio sulla console
        System.out.println("Hello, VS Code!");
    }
}